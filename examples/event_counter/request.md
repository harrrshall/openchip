Implement Verilog module named event_counter with exactly these ports: input clk, input reset_n, input clear, input enable, output [7:0] q. q is an unsigned registered saturating event count. reset_n is active-low asynchronous: asserting reset_n=0 immediately sets q=0 without waiting for a clock edge, regardless of other inputs. Otherwise on each rising clk edge, clear=1 sets q=0 and has priority over enable. With clear=0 and enable=1, increment q unless it is already 255; hold at 255 rather than wrapping. With clear=0 and enable=0, hold q. The reset establishes the initial state. Do not add ports or change reset polarity, asynchronous timing, or priorities.
